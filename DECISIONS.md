# Perfumeries — Decision Log

A running record of locked-in decisions so context survives across sessions. Newest at the top. Nothing here is final until it's written here.

| Date | Area | Decision | Rationale | Status |
|------|------|----------|-----------|--------|
| 2026-08-19 | VIR_Tier POLISH + DEPLOY + RESTART SAVE | Session of UI polish, deployment, and a full save before restart. **Deployment:** repo root `index.html` = self-contained VIR_Tier offline build (GitHub Pages serves root; `.nojekyll` at root); old tool preserved at `perfumeries.html` + `build/`. **Login gate** added (same as Perfumeries: `Finance`/`Pegasus`), wired in app.js + offline shell, light theme, EN/SK/PL/CZ. **Polish:** boot holds ≥6.5s; professional Summary page (monoline SVG icons, restrained type); active sidebar sub-item filled bg; ML finding contract-duration badge (Yearly/Quarterly/Monthly/Half-year/Promo); complexity penalty pushes many-row findings down; AGR-010 pinned to 2nd via `PINNED_SLOTS`; **Finance Overview charts redesigned** (2-per-row, gridlines + value labels + Wong palette, no pastel); finding action buttons brightened + reordered (Review green / Reject-Archive orange / Export outline) + right-aligned blue **Generate Contra-COGS** + amber **Show agreement summary** (opens the agreement doc); Overview top row replaced with the ML portfolio flow (Total → Skipped → Before → After) with € signs. **OPEN DECISION (pending):** users find it unclear where Contra-COGS is generated → researched widely + built a **static concept gallery** `VIR_Tier/concepts/contra_cogs_flow_concepts.html` (A1/A2 loading, B1 canvas, B2 basket [rec], B3 consolidated, B4 board, B5 Claim Builder stage, B6 wizard, B7 sticky). Recommended = A1 + B5/B2 + B3, canvas as drill-down. User to pick, then design/wire. **Config snapshot** of `.kiro/specs/vir-tier-recovery/` + key steering copied into `VIR_Tier/config-snapshot/` (the live `.kiro/` is OUTSIDE the git repo). 59 tests pass. | User polish requests + deployment + save-before-restart | Locked (code) / Pending (Contra-COGS generation rework) |
| 2026-08-19 | PIVOT — new sub-tool | Started **VIR_Tier** recovery sub-tool (`Perfumeries/VIR_Tier/`) based on `PErfumeries.pptx` (slides 1–3). Reframe: from defensive invoice-vs-receipt reconciliation to **offensive recovery of under-claimed CCOGS** — detect missed VIR tier caused by 5 leakage drivers, reconstruct true qualifying volume, compute variance (entitled − claimed), generate **supplementing charges** injected into buyer billing. Existing `build/` tool kept as-is for possible later reuse. | PPTX-defined process + user direction | Locked |
| 2026-08-19 | VIR_Tier scope | SK+PL+CZ with **pan-EU cross-country aggregation** (key new capability). Agreements can be **pan-EU or per-country** (tool detects per agreement). | User answers | Locked |
| 2026-08-19 | VIR_Tier engine | Rebate structure/basis/period/scope all **user-selectable + real-time recompute**, read from agreement data (never hard-coded). Structures: retrospective-tiered, sliding-incremental, flat %, per-unit. Basis: units/value/weight. Period: month/quarter/year. Retrospective reach **both** within-period and prior-period reopening, per invoice/use-case. | User answers | Locked |
| 2026-08-19 | VIR_Tier timing | Rebate volume follows **transfer of control** (IFRS/GAAP received period = Control_Period). **VAT tax point** (EU: shipment or upfront pre-payment) tracked as a **separate, optional** attribute, flagged when it diverges; never assumed (no reliable pre-payment data). Governing definitions surfaced as **hover tooltips** (Regulatory_Note). | User + IFRS/GAAP vs EU VAT distinction | Locked |
| 2026-08-19 | VIR_Tier currency | Native currency per country (SK→EUR, CZ→CZK, PL→PLN); agreement may carry two at once; optional **view-all-in-EUR** FX consolidation; FX required only for value-basis pan-EU with mixed currencies; units/weight aggregate natively. | User answer | Locked |
| 2026-08-19 | VIR_Tier governance | **Show-first** then prompt export-or-inject; **one Finance_Approver** reviews a full document (variance, reconstructed volume, source events, clause, audit trace) before export/injection; immutable audit trace mandatory. Demo simulates billing injection. | User answers | Locked |
| 2026-08-19 | VIR_Tier reuse | Reuse **only proven, non-adapting** pieces: `xml.js` + `csv.js` verbatim; `StateStore`, audit primitive, and i18n mechanism reused but re-configured (i18n extended EN/SK/PL/CZ). All recovery domain logic + UI rebuilt fresh. | User answer #7 | Locked |
| 2026-08-19 | VIR_Tier spec | Created `.kiro/specs/vir-tier-recovery/` — requirements.md (12 reqs, EARS), design.md, tasks.md (18 tasks). Concept map in `Perfumeries/VIR_Tier/CONCEPT.md`. | Requirements-first flow | Locked |
| 2026-08-19 | VIR_Tier BUILT | Implemented full sub-tool under `Perfumeries/VIR_Tier/build/` — engines (consolidation, reconstruction w/ 5 drivers + pan-EU, rebate w/ 4 structures + retro reach, variance, approval, injection), 3 role UIs, EN/SK/PL/CZ i18n, regulatory hover tooltips, **48-case sample dataset** (3× coverage), served + **offline single-file** build. **50 tests passing.** Verified 42 recoverable charges, zero-variance controls, FX EUR-equivalents, approval gating, audit trace, pan-EU win. Old `build/` tool untouched. | User-directed rework; build to runnable demo | Locked |
| 2026-08-19 | VIR_Tier FULL FLOW | Extended beyond the output end to the whole slide flow: (1) **CCOGS Engine output** as a distinct input file = the "before" under-claim; (2) **Before/After + cost-of-inaction** (leakage if nothing done); (3) **ML discovery stage** (`lib/ml.js`) — explainable scoring (magnitude/lift/driver-pressure/tier-proximity) + confidence + reasons + computed/illustrative pattern insights, runs before deterministic suggestions, opt-in; (4) stages Inputs→Discovery→Suggestions→Approval; (5) **light theme** (no black — corrected a regression); (6) **CSV export** primary (billing-ingestible) + audit CSV, JSON dropped as primary. Reqs 13–16 added to spec. **55 tests passing.** | User feedback: build the steps before the output, add ML, fix theme, CSV | Locked |
| 2026-08-20 | VIR_Tier STAGE CONSOLIDATION | Simplified to **3 stages: Inputs → ML Discovery → Finance Overview**. (1) Renamed "Leakage" → **"True-Up"** across ML. (2) Removed **Consolidation** stage; moved its per-country roll-up + goods-receipts-behind-base into the ML finding expand. (3) Removed from ML finding: "Why this priority" signal bars + "Use this suggestion" button. (4) Moved **Approve/Reject/Export/Inject** onto the ML finding via a role-gated Review action → removed the standalone **Finance Approver** stage (role still gates the actions, selected in sidebar). (5) Removed **Analyst Review** stage. (6) Rebuilt **Finance Overview** as a savings/earnings summary (portfolio before→after cards, total True-Up + % uplift, pipeline status, breakdowns by supplier/country/period + top-opportunities table). app.js STAGES trimmed to 3; dashboards.js renderAnalyst/renderApprover/ROLE_RENDERERS removed. 59 tests pass; offline 369 KB; 11-point render check green. | User: consolidate stages — leakage=True-Up, drop Consolidation/Analyst/Approver, actions on ML, Overview = savings/earnings | Locked |
| 2026-08-20 | VIR_Tier REAL-LOSS REWORK | Reworked the tool around the REAL CCOGS-loss situations the customer described. (1) Contracts: flexible windows (month/qtr/half/year), SKU set (=ASIN) with COMBINED/PER_SKU tier measure, engineConfiguredSkus subset (forgotten-SKU), legal metadata. (2) Eligibility = order/invoice date inside contract window; delivery/unload may be later (current engine wrongly needs delivery-note-in-window = the miss). (3) 4 new leakage drivers: EXPIRED_WINDOW_LATE_DELIVERY, FOUND_LATER_PALLET, FORGOTTEN_SKU, REROUTE_SKIPPED_SCAN. (4) True-Up (lib/trueup.js): CCOGS True-Up debit note itemizing each cause as a line, headline = cumulative tier delta (e.g. 1%→2%); flows approval→CSV→injection. (5) Warehouse hierarchy: Country/Main WH → Town WH (WH-PO/WH-ZA); reroute = town WH without main scan. (6) Contract doc view = distinct LEGAL layout (recitals, clauses 1-8, Schedules A/B/C, signatures). (7) Invoice process-journey visual (7 steps, skipped-but-delivered = visible gap). (8) ML recognises the 4 real patterns. Sample generator reworked (17 agreements, per-SKU receipts). 59 tests pass; offline 380 KB; all new views runtime-verified. | User spec update: model real logistics/timing/forgotten-SKU losses + True-Up + contract view + journey visual | Locked |
| 2026-08-19 | VIR_Tier EOD | (1) **Fixed** ML Discovery column shift — findings header + summary now share one `.ml-cols` 6-col grid; chevron moved inside the first cell (was consuming a column and wrapping the 7th value to row 2). Verified 6==6 cells; 59 tests pass; offline rebuilt (339 KB). (2) **PARKED for next session:** user decided the **Analyst Review stage is redundant** — its analysis/points are already in Inputs/Before-After + Consolidation + ML Discovery. Next session: remove stage 4 from `src/app.js` STAGES, decide where the draft-supplementing-charges list lives (ML Discovery → Approver, or a slim Charges view). Not yet implemented — code left intact. | User feedback (column bug + Analyst redundancy) + EOD save request | (1) Locked, (2) Pending |
| 2026-08-19 | VIR_Tier ML+CONSOL CLARITY | Clarified + enriched ML Discovery and Consolidation. ML: legend defining terms; columns relabelled **Original CCOGS (claimed) / Recomputed CCOGS (entitled) / Leakage (recoverable) / Priority** (renamed from "Opportunity" — a 0–100 work-ranking, NOT money, explained on expand); **expandable finding cards** with the full derivation (volume origin base+drivers, before→after money, tier before/after, weighted priority-signal breakdown with points contributed, reasons). `ml.js` findings carry claimed/entitled/leakage/priority/derivation/weights/contrib; runDiscovery returns totalLeakage. Consolidation: added narrative (what/why/how-to-read) + KPI cards + per-agreement reconstruction story, per-country roll-up (pan-EU combine visible), "units restored by drivers — why it was missing" table, GRN receipts behind base. CSS for legend/expandable findings/signal bars. 59 tests pass; offline 339 KB. | User: explain cost-of-inaction/opportunity, show original vs recomputed vs leakage, make rows expandable w/ full calc; enrich consolidation | Locked |
| 2026-08-19 | VIR_Tier AGREEMENT SHEET | Made the **Agreement document view rich + eye-catching** (`agreementDocHtml`): status ribbon, supplier/buyer parties with key-account contacts, term/model meta, 4 headline metric cards, tier-progress bar (achieved tier + units to next), tier-ladder table (achieved row highlighted), **allowance-components table with multiple rows** (VIR / YoY growth / marketing / co-op — researched real rebate-agreement structure), settlement terms, eligible product scope, and an **"Invoices applied under this agreement"** table (per-invoice qty/value + totals). Achieved metrics derived live from pipeline beforeAfter. Added SUPPLIER_CONTACTS + SETTLEMENT_TERMS to companies.js; CSS for ribbon/metric-cards/progress/highlighted tier/pills. 59 tests pass; offline rebuilt + boots clean. | User: make agreements list vendor info + tiers + what was met + applied invoices, be creative/eye-catchy | Locked |
| 2026-08-19 | VIR_Tier REALISM | Reduced Inputs volume **~60%** (48→19 cases). **Broke the 1:1:1**: invoices 45 · delivery notes **19 (consolidated** — one note per multi-country dispatch covering several invoices/warehouses) · goods receipts **81 (out-number** — 1–3 partial GRN receipts per delivery). Added **realistic document rendering** (`src/ui/doc.js` + `src/lib/companies.js`, new perfume SK/PL/CZ company/product reference data): invoice + delivery-note reuse the old tool's realistic layout; **GRN** = standard Goods Received Note (ordered/received/accepted/rejected/condition/inspector/discrepancy); **OSD exception** = reason code + qty affected + backorder + regulatory "why this matters"; **CCOGS Engine** = calc trail + linked docs. GRN + delivery-note fields added to models/parsers. 59 tests pass; offline rebuilt (313 KB) + runtime-verified all 5 renderers. | User: reduce volume 60%, consolidate delivery notes, make inputs look real (reuse old invoice/DN format; research the rest) | Locked |
| 2026-08-19 | VIR_Tier WORKSPACE | Rebuilt UI as a **left-nav workspace** (sidebar L1+L2 nav + wide working area) exposing the full pipeline as navigable stages: **1 Inputs & Collection** (per-category file browser + document modal View/Print/Download of invoices, delivery notes, receipts, missing-data, CCOGS engine files, agreements — reusing old-tool invoice/DN XML formats, new perfume values), **2 Consolidation** (expandable drill-downs on every total), **3 ML Discovery** (Visio-like SVG flow Inputs→Signals→Model→Findings, hover notes, "How it works" method panel), **4 Analyst** (blank-row + `<hr>` dividers between sections), **5 Approver**, **6 Overview**. Added real **invoice + delivery-note** inputs (120 each) and enriched CCOGS engine files with invoice/DN/receipt refs + calc-trail. Free navigation for all roles; approve/reject/export/inject gated to Finance Approver via sidebar role selector. **59 tests passing.** Offline single-file rebuilt (484 KB, 462 data files) + runtime-verified. | User: build more previous sections, left nav, expandable consolidations, ML visual flow, fix back-button/font, dividers | Locked |
| 2026-08-17 | Workspace | Created standalone `Perfumeries/` project, isolated from other workspace tools/steering | New tool, must be online + cloud-capable | Locked |
| 2026-08-17 | Knowledge base | Added `definitions/contra_cogs.md` — multi-source definition of Contra COGS (main process, volume/tiered discounts, invoiced-vs-delivered gap + governance) | Point 0 "definition scraping" — tool needs correct definitions to drive inputs/outputs | Draft (pending user confirmation) |
| 2026-08-17 | Knowledge base | Removed all Amazon/CoOp/1P references from definitions per user request | Tool must stay platform-agnostic | Locked |
| 2026-08-17 | Knowledge base | Added `definitions/use_case_gap.md` — worked 5,000-piece split-delivery example + official gap-fill mechanics (credit note/rebate credit memo vs write-off/shrinkage, accrual + true-up) | Anchor the core contra COGS challenge in a concrete case | Draft (pending user confirmation) |
| 2026-08-17 | Perspective | Confirmed: tool is for the **RECEIVER (buyer)**; contra COGS **already in the invoice**; receiver owns **many storages**; responsibility = track goods, detect & fill gap; user must be able to **choose** resolution options | User clarification | Locked |
| 2026-08-17 | Knowledge base | Enriched definitions into modular files: `glossary.md` (+ resolution-option catalogue), `goods_receipt_and_matching.md`, `stock_transfer.md`, `inventory_write_off.md`, `inventory_shrinkage.md`, `credit_and_debit_notes.md`, `true_up_and_accruals.md`, `contra_cogs_credit.md` | Keep all resolution definitions readable in-tool so user can choose per case | Draft (pending user confirmation) |
| 2026-08-17 | Definitions | Discount confirmed as a **purchase discount** applied upfront on the invoice (NOT a rebate settled later) | User confirmation | Locked |
| 2026-08-17 | Functionality | Added Step 1 "Define functionality (high-level)" to the plan + `FUNCTIONALITY.md`: F1 invoice ingestion, F2 storage ingestion, F3 consolidation (invoice/product-stock-id level), F4 invoice-vs-delivery reconciliation, F5 contra COGS gap engine, F6 open/closed dashboard | User high-level functional description | Draft (pending user confirmation) |
| 2026-08-17 | Knowledge base | Added timing dimension: `goods_in_transit_and_timing.md` + `use_case_timing.md` (cross-month split deliveries, split debits, GINR accrual, FOB/Incoterms, "losing money" mechanisms A/B/C); added F5b Timing/Period Handling to `FUNCTIONALITY.md` | User raised cross-period delivery problem + "how am I losing money" question | Draft (pending user confirmation) |
| 2026-08-17 | Open item → resolved | "Losing money": Incoterms = **FOB shipping point** (buyer bears transit loss); discount = **fixed at invoice level** (not threshold-conditional). ⇒ Exposure is **Mechanism B (lost in-transit goods paid for)** + **C (forgotten value)**; discount forfeiture (A) N/A | User intake answers | Locked (confirm exact Incoterm) |
| 2026-08-17 | Design | Added `DESIGN.md` capturing all intake answers: 3-source scan (DB→API→Folder) with demo "no updates" behavior; folder layout with inbox + **archive**; data model (header-level PO/discount, line-level stock id/qty/price; row-level storage receipts); match on **Stock ID + Invoice number**; total-level discount allocated proportionally for gap valuation; calendar-month timing + GINR + split debits; lifecycle + **backdoor** for lost-forever + **1-manager approval**; **3 role views** (Storage/Accounting/Finance) with login; layered architecture (static demo on GitHub → cloud later) | User answered design questions A–F | Draft (pending confirmation) |
| 2026-08-17 | Hosting | Test version on **GitHub** (static-friendly, no secrets); real DB/API/auth/persistent archive deferred to cloud stage | User direction | Locked (demo) |
| 2026-08-17 | CORE PIVOT | Money-loss reframed: discount is **tiered on total volume** (5,000→1%, 5,000–10,000→1.5%, >10,000→2%), delivered via **proforma invoice** up front; contra COGS obtained on delivery. **Slovak law:** legal invoice only for goods delivered **in the month**. Tiered discount earned only once **full volume delivered** → cross-month splits create an **internal contra COGS credit pending clearance**. Loss = **discount-recognition timing risk** (threshold not met until delivery completes). | User refined understanding | Pending — awaiting company's official process (in Slovak) before rewriting core |
| 2026-08-17 | CONTRADICTION resolved | Not either/or — there are **two models** (per official process): **A) direct line-item** (net price on invoice — the earlier "fixed" case) and **B) back-edge allowance** (standard price + monthly credit note vs central delivery record — the threshold-conditional/pending-credit case). Tool must support **both**; user records which applies. | `official_process_sk.md` | Locked |
| 2026-08-17 | AUTHORITATIVE source | Added `definitions/official_process_sk.md` (company-provided, Slovak). Governs the core: delivery notes per warehouse, **GR via EDI RECADV** as matching basis, per-warehouse or summary invoicing, back-edge settled via **dobropis/Co-op** vs central monthly delivery record | Company process paste | Locked |
| 2026-08-17 | Resolved | Tier %, proforma, month rule are **data-driven per invoice/distributor** (vary by manufacturer) — **not hard-coded**. Read from invoice/distributor data | User confirmation | Locked |
| 2026-08-17 | Demo | **Model selector at startup** (A or B); sample invoices/documents generated to match the chosen model. Both models supported; which applies **depends on the distributor** | User confirmation | Locked |
| 2026-08-17 | Feature | Added **Inventory** sub-module — monthly detail per invoice for **audit** (delivered vs invoiced, contra COGS status, receipts by storage, linked notes, audit trail, exportable). `FUNCTIONALITY.md` F7, `DESIGN.md` §7b | User request | Locked |
| 2026-08-17 | Go | User: "we can move with this if everything else is ready" → prepare schemas + sample data, then scaffold build | User direction | In progress |
| 2026-08-17 | Format | RECADV / goods-receipt feed = **CSV** for the demo; ingestion abstracted so real EDIFACT/XML RECADV can slot in at cloud stage | User confirmation | Locked |
| 2026-08-17 | Spec | Created `.kiro/specs/contra-cogs-reconciliation/` — requirements.md, design.md, tasks.md (16 tasks, format-clean) | Requirements-first spec flow | Locked |
| 2026-08-17 | Stack | UI = **Vanilla JS + ES modules** (no build step); UI isolated so a framework can replace it later without touching engines | User decision | Locked |
| — | Scope | Frontend stack, XML/CSV schemas, cloud target | — | Open (DESIGN §9) |

## Notes
- Decisions move from `Pending` → `Proposed` → `Locked`.
- When a decision changes, add a new row rather than editing the old one (keep the history).

## Session status — 2026-08-17 (end of day)

Tool complete and tested (114 tests). Built beyond the base spec:
- Full-year sample dataset (31 invoices) via `build/tools/generate_samples.js`.
- Animated "connecting to systems" boot sequence (DB → API → warehouses) with real counts; top summary redistributes facts by source.
- i18n EN/SK toggle across the whole UI; About expanded into a full user manual (bilingual).
- Finance charts (bar: risk vs debits; line: value at risk) with the **forecast visually separated** (shaded region + divider + legend + note = "trend if nothing changes").
- Guided "Walk me through" tour.
- Offline single-file build (`build/offline/perfumeries_offline.html`) for visual iteration without git.

**State:** all files saved on disk; local git checkpoint committed. GitHub repo current; Pages **deploy** pending GitHub outage recovery (re-run Actions when green). Resume guide: `RESUME.md`.

**Pending for next session:** finalize boot pacing (`BOOT_STEP_MS`), Storage-view polish, optional Finance breakdowns, accessibility pass, and (future) cloud adapters + real auth.
